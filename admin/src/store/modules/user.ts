import {} from 'lodash';

const state = {
  baseUrl: '/user',
  users: [],
  pagination: {},
  user: null,
  loading: false
};

const actions = {
  
};

const getters = {};

const mutations = {
  
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};